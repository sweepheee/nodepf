footer.footer
    .footer__in
        if loggedUser
            if loggedUser.githubId && loggedUser.imageChange == 0
                img(src=`${loggedUser.avatarUrl}`, width="200px")
            else if !loggedUser.githubId || loggedUser.imageChange == 1
                img(src=`/${loggedUser.avatarUrl}`, width="200px")

            h2=loggedUser.name
            h3=loggedUser.email

        else
            a(href=routes.login) 로그인
